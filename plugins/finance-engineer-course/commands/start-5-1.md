---
description: "Module 5: Build Your Operating System — set up your own workspace, CLAUDE.md, voice, and tools"
---

**Do this SILENTLY:**

1. Read `${CLAUDE_PLUGIN_ROOT}/modules/5-build-your-os/CLAUDE.md` - this is your teaching script
2. Read `${CLAUDE_PLUGIN_ROOT}/SCRIPT_INSTRUCTIONS.md` for critical teaching rules
3. **Course file paths:** whenever this module (or the student) refers to a course file like `modules/.../attachments/...` or `company-context/...`, resolve it relative to `${CLAUDE_PLUGIN_ROOT}/`. The student's own files and exercise outputs live in their current working directory.
4. Follow the teaching script precisely:
   - Execute "Say:" blocks word-for-word
   - Stop at "Check:" points and wait
   - Run "Action:" blocks exactly as specified
   - Start teaching immediately (no meta-commentary)
